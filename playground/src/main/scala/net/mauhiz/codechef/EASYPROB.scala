package net.mauhiz.codechef

object EASYPROB extends App {
  def form(source: Int): String = {
    throw new Exception
  }
  Seq(137, 1315, 73, 136, 255, 1384, 16385).foreach { x: Int ⇒ println(form(x)) }
}